{
  "name": "ai-dreams-hackathon",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node dev.js",
    "build": "node -e \"import('child_process').then(c => {try{c.execSync('tsc && vite build || next build || react-scripts build', {stdio:'inherit'})}catch(e){console.log('⚠️ Build fallback: no matching framework found')}})\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "express": "^5.1.0",
    "next": "^16.0.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "vite": "^7.2.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
